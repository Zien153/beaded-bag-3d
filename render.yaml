services:
  - type: web
    name: browser-use-remote-mcp
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: BROWSER_USE_API_KEY
        sync: false
      - key: MCP_PATH_TOKEN
        sync: false
